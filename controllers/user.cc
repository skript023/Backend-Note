#include "user.h"

// Add definition of your processing function here
namespace gottvergessen
{
    void user::find_all(HttpRequestPtr const& req, func_t&& callback)
    {

    }
    void user::find_by_id(HttpRequestPtr const& req, func_t&& callback, std::string&& id)
    {

    }
    void user::create(HttpRequestPtr const& req, func_t&& callback)
    {

    }
    void user::update(HttpRequestPtr const& req, func_t&& callback, std::string&& id)
    {

    }
    void user::remove(HttpRequestPtr const& req, func_t&& callback, std::string&& id)
    {
        
    }
} // namespace gottvergessen
